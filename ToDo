1. **JOIN:**
   - Verificar se o canal é válido.
   - Verificar se o canal possui uma senha (se aplicável).
   - Criar um índice para o canal e sua senha (se existir).
   - Verificar se há vários canais (sempre separados por vírgula e sem espaços).
   - Substituir vírgulas por espaços no input.
   - Criar o vetor (ss).
   
2. **KICK:**
   - Verificar se o canal está no vetor.
   - Verificar se o canal é válido.
   - Verificar se o canal possui uma senha (se aplicável).
   - Verificar se o nick está no canal.
   - Substituir vírgulas por espaços no input.
   - Criar o vetor (ss).
   
3. **LIST:**
   - Substituir vírgulas por espaços no input.
   - Criar o vetor (ss).
   
4. **NICK:**
   - Substituir vírgulas por espaços no input.
   - Criar o vetor (ss).
   
5. **PART:**
   - Substituir vírgulas por espaços no input.
   - Criar o vetor (ss).
   
6. **PRIVMSG:**
   - Substituir vírgulas por espaços no input.
   - Criar o vetor (ss).
   
7. **QUIT:**
   - Substituir vírgulas por espaços no input.
   - Criar o vetor (ss).
   
8. **TOPIC:**
   - Substituir vírgulas por espaços no input.
   - Criar o vetor (ss).
   
9. **MODE:**
   - Substituir vírgulas por espaços no input.
   - Criar o vetor (ss).
   
10. **INVITE:**
    - Substituir vírgulas por espaços no input.
    - Criar o vetor (ss).
    
11. **WHO:**
    - Substituir vírgulas por espaços no input.
    - Criar o vetor (ss).
   

   A FAZER

   Mudar TOPIC so como Operador ou se canal esta com TOPIC aberto
   - Falta mandar msg TOPIC para todos
  
   DONE - falta testar melhor:
   1. O /mode +k password" coloca paassword no canal, mas ainda nao restringe os users e operadores sem pass 
   2. Um canal pode ser privado e restrito, se tiver convite mas nao tiver pass, nao entra
   3. o Operador tem de entrar automaticamente na lista de invitedUsers e enquanto for operador nunca perde o invite para puder entrar e sair do canal quando quer
   
   POR RESOLVER: EM ABERTO

   4. Invite so pode ser dado por Operador
      c. exception encontrada como bad_alloc no invite nessa parte possivelmente:
         const std::map<std::string, Channel>::iterator it = getChannels().find(channelName);
         try {
         if(!Utils::isOperator(it->second, client.getNick()))
         {
            SEND(client.getSocket(), ERR_CHANOPRIVSNEEDED(client, getInput()[2]), "Error sending ERR_CHANOPRIVSNEEDED (482)");
            return;
         }
         } catch (std::exception& ex)
         {
            std::cerr << ex.what() << std::endl;
         }

   5. Invite nao pode ser dado ao mesmo client que esta convidando POR RESOLVER

      a. problemas encontrados:
      - esta printando  GARBAGE: c1r12s4.42porto.com443 ao fazer o SEND abaixo:

      if (getInput()[1] == client.getNick())
		{
			SEND(client.getSocket(), ERR_USERONCHANNEL(client.getNick(), client.getNick(), channelName), "Error sending ERR_CHANOPRIVSNEEDED (482)");
			return;
		}

      b. se o user e um operador do canal mas ele saiu do canal e esta dando um invite a ele mesmo a mensagem precisa ser diferente de ":is already on channel"
